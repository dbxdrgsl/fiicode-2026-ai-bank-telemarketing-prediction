# Docs

This folder expands on the short instructions in the main README.

## Files

- `final_094939_solution.md`
  Explains what the final `0.94939` solution does, why `exp058` was the anchor,
  why the `exp062` experiment mattered, and how `exp064b` was constructed.

- `final_094939_rerun.md`
  Gives the exact commands and generation steps needed to rebuild the tracked
  `exp064b` artifact bundle from the saved source predictions.

## Recommended Reading Order

1. Read `final_094939_solution.md` to understand the method.
2. Read `final_094939_rerun.md` to reproduce the files.
3. Use the top section of `README.md` for the shortest rerun path.
